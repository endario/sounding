# The model each provider offers at each tier, promotions, bans, preferences and vendors' published
# figures. A provider is the maker of the models listed under it; `usage` is the vendor whose
# account a run spends. Override on one machine, without a release, in
# $XDG_CONFIG_HOME/unlimited/catalog.toml (default ~/.config/unlimited/catalog.toml).
schema = 1

# The tiers a provider may list a model at: capability classes, named by whoever keeps this file.
# A local list replaces this one.
tiers = ["standard", "heavy"]

# Model ids never offered, wherever they are listed. A local list adds to this one.
banned = []

# Providers preferred among near-equal candidates, most preferred first (here the cheapest). A local
# list replaces this one.
tie_preference = ["meta", "deepseek", "stealth"]

[providers.codex]
usage = "openai"
standard = "gpt-6-luna"
heavy = "gpt-6-sol"

[providers.claude]
usage = "anthropic"
standard = "sonnet"
heavy = "opus"

[providers.glm]
usage = "zai"
standard = "glm-5.3-flash"
heavy = "glm-5.3"

[providers.grok]
usage = "xai"
standard = "grok-4.7"

[providers.deepseek]
usage = "opencode"
standard = "opencode-go/deepseek-v4.1-flash"

[providers.meta]
usage = "opencode"
standard = "opencode-go/muse-spark-1.3-contributor"

# Unnamed preview models, whoever makes them.
[providers.stealth]
usage = "commandcode"

# Models offered at a tier for a limited time, without quota cost. `until` is optional: live through
# that day, UTC.
[[promotions]]
provider = "stealth"
model = "commandcode/stealth/space-bunny-alpha"
tiers = ["standard"]
until = 2026-09-30

# What a vendor publishes about a model it sells: an expected figure until our own attempts say
# otherwise (`unlimited cards`). Price and speed are the vendor's, not the model's: `models` are the
# ids this catalog runs the model under, on any route, and a route whose own vendor has no card
# shows another vendor's as a guideline only. Prices are USD per million tokens; `intelligence` is
# the vendor's quoted index. A local card replaces the shipped one of the same vendor, name and plan.
[[cards]]
vendor = "commandcode"
plan = "goat"
name = "Space Bunny Alpha"
models = ["commandcode/stealth/space-bunny-alpha"]
price = { input = 0.0, output = 0.0, cache_read = 0.0 }
source = "https://commandcode.ai/docs/plans/goat"
as_of = 2026-09-26

[[cards]]
vendor = "commandcode"
plan = "goat"
name = "GPT-6 Luna"
models = ["gpt-6-luna"]
intelligence = 37.3
tok_s = 155
price = { input = 0.10, output = 0.50, cache_read = 0.01, cache_write = 0.125 }
source = "https://commandcode.ai/docs/plans/goat"
as_of = 2026-09-26

[[cards]]
vendor = "commandcode"
plan = "goat"
name = "GLM-5.3"
models = ["glm-5.3"]
intelligence = 44.8
tok_s = 63
price = { input = 1.40, output = 4.40, cache_read = 0.26 }
source = "https://commandcode.ai/docs/plans/goat"
as_of = 2026-09-26

[[cards]]
vendor = "commandcode"
plan = "goat"
name = "GLM-5.3 Flash"
models = ["glm-5.3-flash"]
intelligence = 41.8
tok_s = 49
price = { input = 0.15, output = 0.50, cache_read = 0.03 }
source = "https://commandcode.ai/docs/plans/goat"
as_of = 2026-09-26

# List price; the page showed 40% off until 2026-09-27.
[[cards]]
vendor = "commandcode"
plan = "goat"
name = "Grok 4.7"
models = ["grok-4.7"]
intelligence = 46.4
tok_s = 50
price = { input = 2.00, output = 6.00, cache_read = 0.50 }
source = "https://commandcode.ai/docs/plans/goat"
as_of = 2026-09-26

# Off-peak price (17 h a day); peak is 0.30 / 1.20.
[[cards]]
vendor = "commandcode"
plan = "goat"
name = "DeepSeek V4.1 Flash"
models = ["opencode-go/deepseek-v4.1-flash"]
intelligence = 39.5
tok_s = 237
price = { input = 0.15, output = 0.60, cache_read = 0.003 }
source = "https://commandcode.ai/docs/plans/goat"
as_of = 2026-09-26

[[cards]]
vendor = "commandcode"
plan = "goat"
name = "Muse Spark 1.3 Contributor"
models = ["opencode-go/muse-spark-1.3-contributor"]
intelligence = 48.1
tok_s = 248
price = { input = 0.10, output = 0.20, cache_read = 0.002 }
source = "https://commandcode.ai/docs/plans/goat"
as_of = 2026-09-26
