[project]
name = "unlimited"
version = "0.0.17"
description = "Read AI-subscription quota usage, per window, as facts — and where each window is heading."
requires-python = ">=3.11"
license = "MIT"
readme = "README.md"
dependencies = []

[project.scripts]
unlimited = "unlimited.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
